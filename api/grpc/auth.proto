syntax = "proto3";

package auth;

option go_package = "pkg/grpc";


message AuthRequest {
  string username = 1;    
  string password = 2;    
}

message AuthResponse {
  string token = 1; 
}


service AuthService {
  rpc Auth(AuthRequest) returns (AuthResponse);
}